name: unit test

on:
  push

jobs:
  unittest:
    runs-on: ubuntu-latest
    name: 'Unit Test'
    steps:
      - name: Python Unit Test
      - run:
          using: docker
          image: './Dockerfiles/test_Dockerfile'
